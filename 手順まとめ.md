# あなたがやること — 手順まとめ

このプロジェクトで**自分で設定・挿入する必要があること**をチェックリスト形式でまとめています。

---

## 1. Firebase の設定（必須）

### 1-1. Firebase プロジェクトの準備

1. [Firebase Console](https://console.firebase.google.com/) でプロジェクトを作成する。
2. **Realtime Database** を作成する。（Build → Realtime Database → データベースの作成）
3. **Authentication** で **匿名** を有効にする。（Build → Authentication → 始める → 匿名を有効化）
4. **プロジェクトの設定**（歯車）→ **全般** → 「アプリを追加」→ **Web** を選び、表示された `firebaseConfig` を控える。

### 1-2. フロント用の API 設定を挿入

**ファイル:** `public/js/firebase-config.js`

次のプレースホルダを、Firebase Console で表示された実際の値に**置き換える**。

| プレースホルダ | 置き換える内容 |
|----------------|----------------|
| `YOUR_API_KEY` | Firebase の apiKey |
| `YOUR_PROJECT_ID` | プロジェクト ID（apiKey 以外の箇所にも複数ある） |
| `YOUR_SENDER_ID` | messagingSenderId |
| `YOUR_APP_ID` | appId |

- `authDomain` は `YOUR_PROJECT_ID.firebaseapp.com`
- `databaseURL` は `https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com`
- `storageBucket` は `YOUR_PROJECT_ID.appspot.com`

※ 本番では `database.rules.json` を Firebase Console の Realtime Database → ルールに反映する。

---

## 2. Gemini API の設定（キャラ画像生成を使う場合のみ）

### 2-1. API キーを取得

1. [Google AI Studio](https://aistudio.google.com/) または Google Cloud Console で **Gemini API** を有効化する。
2. **API キー** を発行する。

### 2-2. API キーをプロジェクトに挿入（どちらか一方でOK）

**方法 A: .env ファイル（推奨）**

1. プロジェクトルート（`SKD404/`）に **`.env`** を作成する。
2. 次の1行を書く（`your_gemini_api_key_here` を実際のキーに置き換え）。

   ```
   GEMINI_API_KEY=your_gemini_api_key_here
   ```

3. **`.env` は git にコミットしない**（すでに `.gitignore` に含まれています）。

**方法 B: 環境変数**

- 開発サーバーを起動する**前**に、ターミナルで次を実行する。

  **Windows (PowerShell):**

  ```powershell
  $env:GEMINI_API_KEY = "あなたのAPIキー"
  ```

  **Windows (コマンドプロンプト):**

  ```cmd
  set GEMINI_API_KEY=あなたのAPIキー
  ```

---

## 3. 開発サーバーの起動

プロジェクトルート（`SKD404/`）で次を実行する。

```bash
php -S localhost:8000 router.php
```

ブラウザで **http://localhost:8000** を開く。

- 初回は `login.html`（プロフィール登録）に飛ぶ。
- 名前・学年・得意教科・趣味を入力して「登録してはじめる」→ ホーム（`index.html`）へ遷移。
- フッターで **ニュース / 記録 / 学習 / 設定** を確認。

---

## 4. 動作確認のチェックリスト

| 確認項目 | やること |
|----------|----------|
| ログイン | プロフィール登録後、ホームに遷移するか |
| 記録 | 記録画面でメモを保存し、一覧に表示されるか |
| 設定 | 登録データが表示され、ログアウトできるか |
| Gemini（任意） | キャラ画像生成を使う場合、`POST /api/gemini.php` が動くか（ホームで画像表示する実装を入れた後） |

---

## 5. デプロイする場合（Firebase Hosting）

1. Firebase CLI を入れる: `npm install -g firebase-tools`
2. ログイン: `firebase login`
3. プロジェクト指定: `firebase use <あなたのプロジェクトID>`
4. デプロイ: `firebase deploy`

※ PHP（`api/gemini.php` など）は Firebase Hosting では動きません。フロントと Firebase（認証・DB）のみ。Gemini 用 API は別サーバーや Cloud Functions などにデプロイが必要です。

---

## まとめ — 最小限やること

1. **Firebase** … Console でプロジェクト作成・Realtime DB・匿名認証を有効化。
2. **`public/js/firebase-config.js`** … `YOUR_*` を実際の Firebase の値に置き換え。
3. **（任意）Gemini** … API キーを取得し、`.env` に `GEMINI_API_KEY=...` を書く。
4. **起動** … `php -S localhost:8000 router.php` でサーバーを起動し、ブラウザで `http://localhost:8000` を開く。

詳細は **SETUP.md** を参照してください。
